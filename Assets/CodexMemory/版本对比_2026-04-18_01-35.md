# 版本对比（Scripts C#）

- 触发口令：[生成当前版本快照并与上次进行对比]
- 对比范围：Assets/Scripts/**/*.cs
- 上次快照提交：3049364（2026-04-17）
- 本次快照提交：1060b26（2026-04-18）

## 1) 新增/修改脚本清单
- CharacterController/StateMachine/BeHitState.cs（新增）
- CharacterController/StateMachine/CharacterStateControl.cs
- CharacterController/Event/CharacterEvent.cs
- CharacterController/StateMachine/DodgeState.cs
- CharacterController/Input/CharacterControlDriver.cs

## 2) 本次新增功能点（按模块）
- 受击状态模块
  - 新增 BeHitState，实现进入受击时触发受击动画、开启根运动、结束后退出。
  - 新增 canExitBeHit 出口控制，配合动画事件完成状态释放。
- 状态机核心
  - 新增 hasPendingHit 与 RequestBeHit()，用于由外部系统请求受击。
  - 在 OnTick() 中加入受击优先级切换逻辑，并根据战斗姿态恢复到 FightMove/FightRun 或 Move/Run。
- 动画事件总线
  - 将闪避结束事件扩展为通用事件：DodgeOrBeHitEndEvent，用于闪避/受击共享结束通知。
- 驱动调试入口
  - CharacterControlDriver 增加 P 键测试入口，便于手动触发受击流程验证。

## 3) 本次完成实现点（行为与结果）
- 可以通过代码请求进入受击状态（RequestBeHit）。
- 受击动画结束后可回到对应移动状态。
- 闪避与受击的结束事件在 CharacterEvent 中统一接线。
- 角色驱动层具备受击流程手动回归测试入口。

## 4) 关键逻辑变更与影响范围
- CharacterStateControl 增加了“受击优先级”分支，影响全部状态切换仲裁顺序。
- CharacterEvent 事件命名变化（DodgeEndEvent -> DodgeOrBeHitEndEvent），会影响所有订阅端。
- DodgeState 已切换到新事件名，避免事件接口不一致导致闪避无法退出。

## 5) 待验证项与建议下一步
- 枚举一致性：StateType 新增了 BeHit，但切换代码仍使用 StateType.Hit；建议统一为一个枚举值，避免运行时找不到状态。
- 触发重复：CharacterStateControl 在进入受击时先 nimator.SetTrigger(AnimBeHit)，BeHitState.Enter() 里又触发一次，建议保留一处。
- 事件拆分：当前闪避与受击共用一个结束事件，后续建议拆成 DodgeEndEvent 与 BeHitEndEvent 降低误触发风险。
- 打断策略：建议补上“不可受击窗口”条件（无敌帧/霸体/死亡态），避免任何时刻都可被强打断。

## 6) 统计与执行备注
- 变更统计：5 files changed, 108 insertions(+), 7 deletions(-)
- 本地提交已完成：1060b26
- git push 失败：仓库未配置远端（No configured push destination）
