# 版本对比（Scripts C#）

- 对比对象：上次提交 `390d032` vs 上上次提交 `1060b26`
- 对比范围：`Assets/Scripts/**/*.cs`
- 生成时间：2026-04-18 21:38:16

## 1) 新增/修改脚本清单
- `CharacterController/Input/CharacterControlDriver.cs`
- `CharacterController/StateMachine/AttackState.cs`
- `CharacterController/StateMachine/CharacterStateControl.cs`

## 2) 本次新增功能点（按模块）
- 状态机层（CharacterStateControl）
  - 从扁平条件链改为父层枚举驱动：新增 `SuperStateType`（Locomotion/Action/Interrupt）。
  - 新增动作请求枚举 `ActionRequest` 与动作解析函数。
  - 新增父层仲裁函数与父层切换函数（`ChooseSuperState`、`SwitchSuperState`）。
  - 新增“非 Attack 状态下攻击输入仅缓存一次”的限制（`_nonAttackQueuedOnce`）。
  - 在受击父层切换逻辑中屏蔽攻击输入，避免受击期间推进连段。
- 攻击状态层（AttackState）
  - 在 `Enter()` 连段推进分支后补充 `_chainConsumedInCurrentStep = true`，减少 Enter/Update 双推进导致的跳段。
  - 同步记录 `_lastStateFullPathHash` 以配合 Update 解锁逻辑。
  - 增加了若干调试日志（第一次攻击、接续攻击）。
- 驱动层（CharacterControlDriver）
  - `BeHit` 状态注册枚举从 `StateType.Hit` 修正为 `StateType.BeHit`。

## 3) 本次完成实现点（行为与结果）
- 状态机进入了“父级仲裁 + 子状态切换”的结构化实现雏形。
- 同帧切换增加封口 `return`，减少多次切换抖动。
- 连招系统针对“非攻击态狂按”加入节流，避免预输入导致过度推进。
- 修复了 `AttackReset` 中 `_comboRuntime` 空引用风险。
- 武器切换添加了索引越界保护。

## 4) 关键逻辑变更与影响范围
- `CharacterStateControl.OnTick()` 主流程重构，影响所有状态切换路径。
- `StateChange()` 新增防御判断与父级状态同步，影响状态转换一致性。
- 受击输入处理从“全局逻辑”迁移到父级切换流程，影响 BeHit 期间攻击输入行为。
- `AttackState.Enter()` 改动后，首段/接续段的推进时机与过去不同，需重点回归连段起手与循环边界。

## 5) 待验证项与建议下一步
- 验证“Move 首击是否仍会误走接续段（1/2）”问题是否彻底消失。
- 评估 `AttackState` 中新增 `Debug.Log` 是否需要保留（建议加开关或移除）。
- 检查 `CharacterStateControl` 中 `StateChange()` 与 `SetState()` 的父级同步是否与所有状态类一致。
- 建议下一步把 `Dodge` 的 Animator 触发统一收敛到状态机一处，避免 Driver 与 State 双触发。

## 6) 统计
- `3 files changed, 261 insertions(+), 130 deletions(-)`
