# 版本对比（Scripts C#）

- 触发口令：`[生成当前版本快照并与上次进行对比]`
- 对比基线：`390d032`
- 本次快照：`067052e`
- 对比范围：`Assets/Scripts/**/*.cs`

## 1) 新增/修改脚本清单
- 修改：
  - `CharacterController/Input/CharacterControlDriver.cs`
  - `CharacterController/StateMachine/CharacterStateControl.cs`
  - `CharacterController/StateMachine/MoveState.cs`
  - `CharacterController/StateMachine/RunState.cs`
  - `CharacterController/StateMachine/FightMoveState.cs`
  - `CharacterController/StateMachine/FightRunState.cs`
- 新增：
  - `UI/Core/IUIController.cs`
  - `UI/Core/UIControllerBase.cs`
  - `UI/Core/UIManager.cs`
  - `UI/Core/UIModelBase.cs`
  - `UI/Core/UIPanelEntry.cs`
  - `UI/Core/UIPanelId.cs`
  - `UI/Core/UIViewBase.cs`

## 2) 本次新增功能点（按模块）
- 角色移动相机对齐（CharacterController）
  - 为 Driver 增加 `cameraTransform` 引用并在空值时回退 `Camera.main`。
  - `MoveState/RunState` 及战斗移动状态构造注入相机引用。
  - 移动输入从“世界轴”改为“相机平面前/右方向”，支持以镜头前方为角色前进方向。
- 状态机结构化（CharacterStateControl）
  - 引入 `SuperStateType` / `ActionRequest`，主循环改为父级仲裁 + 子状态切换。
  - 加入同帧封口 `return`，减少一帧内多次切换。
  - 增加非攻击态攻击输入节流（`_nonAttackQueuedOnce`）。
  - 增加受击态攻击输入屏蔽、`_comboRuntime` 空判、武器切换索引防越界。
- UI 框架基础（UI/Core）
  - 新增 MVC 基础接口与基类：Model/View/Controller。
  - 新增 `UIManager` 面板管理：缓存、绑定控制器、开关、解绑、销毁。
  - 新增面板ID与面板注册结构体。

## 3) 本次完成实现点（行为与结果）
- 角色移动方向可按相机朝向驱动，改善自由镜头下的操控一致性。
- 状态机从扁平链路向分层仲裁演进，切换路径更可控。
- 已具备可复用的 UI MVC 框架核心能力，可在后续快速扩展面板。

## 4) 关键逻辑变更与影响范围
- `CharacterStateControl.OnTick()` 主流程重构，影响攻击/跳跃/闪避/受击全链路。
- `MoveState/RunState` 输入坐标系变更，影响所有移动与朝向表现。
- UI 新框架已入库，但与场景接线和跨场景生命周期策略仍需进一步统一。

## 5) 待验证项与建议下一步
- 回归验证：
  - FreeLook 下角色前进方向是否稳定与主相机一致。
  - 受击期间攻击输入是否完全被屏蔽且退出后恢复正常。
  - 非攻击态狂按攻击是否只触发一次进入攻击。
- UI 侧建议：
  - 明确 `UIManager` 的跨场景策略（常驻或场景级）。
  - 统一 `OpenPanel/ClosePanel` 与 `ShowPanel/HidePanel` 命名，减少接口歧义。

## 6) 统计与执行备注
- 变更统计：`13 files changed, 348 insertions(+), 19 deletions(-)`
- 本地提交：`067052e`
- `git push` 失败：未配置远端（`No configured push destination`）
